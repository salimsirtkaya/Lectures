function PARTICLE_SET = InitParticles(no_of_particles)

for i=1:no_of_particles
    PARTICLE_SET.particles(i).pose(1)=0; % x position
    PARTICLE_SET.particles(i).pose(2)=0; % y position
    PARTICLE_SET.particles(i).pose(3)=-270; % orientation