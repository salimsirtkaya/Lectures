
function genPath()

pose(1,:) = [0 0 pi/2];

[Measurement,poseUpdate,tMax] = LoadTestData2(0);

for t=2:30
    pose(t,:) = UpdatePose_withoutnoise(pose(t-1,:),poseUpdate(t-1,:));
end

figure
AXIS([-20 100 -275 150]);
plot(pose(:,1),pose(:,2));
grid;
hold;

